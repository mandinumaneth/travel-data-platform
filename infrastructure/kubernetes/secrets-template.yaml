# This file must contain real values in base64 before apply and must never be committed to Git.
apiVersion: v1
kind: Secret
metadata:
  name: travel-secrets
type: Opaque
data:
  SNOWFLAKE_PASSWORD: BASE64_ENCODED_SNOWFLAKE_PASSWORD
  POSTGRES_PASSWORD: BASE64_ENCODED_POSTGRES_PASSWORD
  KAFKA_BOOTSTRAP_SERVERS: BASE64_ENCODED_KAFKA_BOOTSTRAP_SERVERS
